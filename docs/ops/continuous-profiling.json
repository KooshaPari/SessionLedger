{
  "schema": "sessionledger.continuous-profiling.v1",
  "sample_seconds": 1,
  "poll_interval_seconds": 30,
  "retain_samples": 3,
  "push_backend": "none",
  "notes": [
    "Stub agent config for L45 continuous profiling evidence — not a production sidecar.",
    "On-demand unix CPU protobuf sampling landed in Wave-27 (#232); this stub polls the same loopback surface.",
    "push_backend remains none until Pyroscope/OTLP profiling export is wired (unpaid).",
    "Windows hosts skip CPU sampling (pprof-rs needs SIGPROF); CI agent job is ubuntu-only soft evidence.",
    "Scheduled soft job delegates to scripts/continuous-profiling-agent.ps1 and reuses pprof-smoke contracts."
  ]
}
