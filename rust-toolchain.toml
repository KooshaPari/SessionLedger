[toolchain]
# Exact rustc pin (C06 L60). Do not use floating `stable` / `beta` / `nightly`
# here — bump channel + docs/ops/rustc-toolchain-pin.json together.
# MSRV remains workspace Cargo.toml `rust-version = "1.85"`.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
