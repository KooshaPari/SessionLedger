CLUSTER_START cluster=C06 repo=SessionLedger pillars=L51-L60 date=2026-07-05

### L51 — Pinned dependencies
score: 1  glyph: △
evidence:
  - Cargo.toml:1-29 pins the workspace package metadata and dependency versions, and README.md:64-70 documents the build/test loop
  - LICENSE files exist (LICENSE-MIT, LICENSE-APACHE), but find . -maxdepth 2 for Cargo.lock / rust-toolchain.toml / deny.toml returned no lockfile or toolchain pin
gaps:
  - No Cargo.lock or rust-toolchain.toml in repo root — effort: S
soft_goal_delta: version pins exist in Cargo.toml, but lockfile/toolchain verification is missing

### L52 — Reproducible builds
score: 0  glyph: ✗
evidence:
  - .github/workflows/ci.yml:13-29 runs fmt/clippy/build/test, but does not set SOURCE_DATE_EPOCH or any reproducibility check
  - .github/workflows/release.yml:61-145 packages release binaries, but no bit-identical rebuild or checksum verification step is present
gaps:
  - No reproducible-build policy or verify job — effort: M
soft_goal_delta: release artifacts are produced, not proven bit-identical

### L53 — Build provenance
score: 0  glyph: ✗
evidence:
  - .github/workflows/release.yml:61-145 uploads binaries via action-gh-release, but no SLSA provenance or GitHub OIDC attestation step appears
  - rg for slsa / attest / provenance / cosign in README.md, Cargo.toml, .github, packaging, docs, src, tests found only narrative provenance language in docs/DESIGN.md:52-54
gaps:
  - No SLSA provenance generation for release artifacts — effort: L
soft_goal_delta: release flow exists, but provenance is not emitted

### L54 — Hermetic builds
score: 0  glyph: ✗
evidence:
  - packaging/README.md:33-35 explicitly relies on cargo build --release and says nothing about offline or no-network builds
  - .github/workflows/ci.yml:17-29 uses the standard Rust toolchain and cache, with no network isolation or vendoring controls
gaps:
  - No no-network build mode or CI enforcement — effort: M
soft_goal_delta: build is conventional, not hermetic

### L55 — Dependency confusion guard
score: 0  glyph: ✗
evidence:
  - rg for cargo-deny / deny.toml / private registry / typosquat / scoped registry returned no hits in repo root, .github, packaging, docs, src, or tests
  - Cargo.toml:15-20 depends on crates.io packages directly without a repository-local registry policy
gaps:
  - No cargo-deny config or registry-scoping policy — effort: M
soft_goal_delta: dependency sources are unguarded

### L56 — Container provenance
score: 0  glyph: ✗
evidence:
  - rg for cosign / sigstore / attest / container provenance in repo returned no workflow or docs hits
  - .github/workflows/release.yml:61-145 packages binaries, but no container image build/sign/verify lane exists
gaps:
  - No signed container image or attestation pipeline — effort: L
soft_goal_delta: this repo ships binaries, not attestable containers

### L57 — MCP server provenance
score: 0  glyph: ✗
evidence:
  - rg for phenoMCP / MCP server pin / server provenance returned no hits in repo root, docs, or workflows
  - README.md:49-62 and docs/DESIGN.md focus on sessions, distillation, and viewer composition, not server pinning
gaps:
  - No pinned/attested MCP server manifest — effort: M
soft_goal_delta: no server provenance surface present

### L58 — License scan
score: 1  glyph: △
evidence:
  - Cargo.toml:11 declares MIT OR Apache-2.0, and LICENSE-MIT / LICENSE-APACHE exist at repo root
  - rg for gitleaks / trufflehog / pre-commit / license scan found no in-repo scan wiring, and .github/workflows/ci.yml has only fmt/clippy/build/test
gaps:
  - No automated allow-list or copyleft scan in CI — effort: M
soft_goal_delta: clear license choice exists, but no scan gate

### L59 — Source code provenance
score: 0  glyph: ✗
evidence:
  - rg for signed commits / branch protection / commit signing returned no repo-local policy or workflow evidence
  - README.md:64-70 and .github/workflows/ci.yml:13-29 cover build/test only, with no author-signing enforcement
gaps:
  - No commit-signing or branch-policy evidence in-repo — effort: L
soft_goal_delta: source provenance is not enforced

### L60 — Toolchain pinning
score: 1  glyph: △
evidence:
  - Cargo.toml:8-9 pins rust-version = 1.85, and .github/workflows/{ci,qgate,release}.yml all use dtolnay/rust-toolchain@stable
  - find . -maxdepth 2 for rust-toolchain.toml / .tool-versions returned no repo-local pin file
gaps:
  - Add rust-toolchain.toml or equivalent local pin with CI verification — effort: S
soft_goal_delta: minimum Rust version is pinned, but the toolchain itself is not

CLUSTER_TOTAL score=2/30 pct=7% grade=F
CLUSTER_DONE cluster=C06 repo=SessionLedger
