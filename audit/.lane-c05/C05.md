CLUSTER_START cluster=C05 repo=SessionLedger pillars=L41-L50 date=2026-07-10

### L41 — Structured logs
score: 2  glyph: ~
evidence:
  - crates/sl-daemon/Cargo.toml:28-30 — tracing + tracing-subscriber (env-filter, fmt) dependencies
  - crates/sl-daemon/src/main.rs:313-319 — init_tracing() installs EnvFilter subscriber (RUST_LOG / default sl_daemon=info)
  - crates/sl-daemon/src/http.rs:41,90-97 — tracing spans on healthz/readyz and HTTP handlers
  - crates/sl-daemon/src/worker.rs:8,18 — worker/process_session instrumented spans
  - docs/ops/observability.md:122-134 — log-level discipline / RUST_LOG guidance
  - MISSING: JSON formatter / logfmt-pretty for all binaries (fmt text only today)
gaps:
  - Enable JSON (prod) + logfmt-pretty (dev) subscriber features — effort: S
soft_goal_delta: +55% (tracing wired in daemon; JSON/logfmt-pretty absent)

### L42 — OpenTelemetry instrumentation
score: 1  glyph: △
evidence:
  - docs/ops/observability.md:99-118 — intended OTel soft goal (tracing + OTLP + traceparent) via issue #65
  - src/ports/mod.rs:7-8 — TraceSink documented as pheno-tracing TracePort boundary
  - docs/DESIGN.md:154 — Observability row cites pheno-tracing / PhenoObservability
  - MISSING: opentelemetry / otlp dependency or SDK wiring in Cargo.toml / crates/*/Cargo.toml
gaps:
  - Add OTel SDK + OTLP exporter behind TraceSink adapter — effort: L
soft_goal_delta: +25% (ops soft-goal doc + port designed; SDK absent)

### L43 — Metrics emission
score: 2  glyph: ~
evidence:
  - crates/sl-daemon/src/metrics.rs:1-29 — GET /api/metrics computes bundle/token/model histograms
  - crates/sl-daemon/src/http.rs:65 — routes /api/metrics handler
  - docs/ops/observability.md:65-82 — RED metrics mapping to current signals + stub exporter names
  - docs/ops/runbook.md:68-76 — curl /api/metrics operator check
  - MISSING: prometheus / opentelemetry-metrics / statsd exporters
gaps:
  - Emit Prometheus/OTLP counters+gauges (RED/USE) alongside app metrics — effort: M
soft_goal_delta: +50% (HTTP metrics + RED map docs; exporters absent)

### L44 — Distributed trace propagation
score: 0  glyph: ✗
evidence:
  - docs/ops/observability.md:111 — W3C traceparent listed as not done
  - MISSING: traceparent / tracestate handlers in HTTP middleware
  - src/ports/mod.rs:68-71 — TraceSink has no context-propagation API
  - crates/sl-daemon/src/http.rs:52-65 — axum router has no W3C trace middleware
gaps:
  - Propagate W3C traceparent across HTTP and pipeline stages — effort: M
soft_goal_delta: +5% (documented soft goal only)

### L45 — Continuous profiling
score: 0  glyph: ✗
evidence:
  - MISSING: pyroscope / pprof / continuous profiling — repo search (excl. audit/) returned 0 matches
  - .github/workflows/ci.yml:12-41 — no profiling or flamegraph job
  - .github/workflows/nightly-mutants.yml:1-28 — mutation testing only; not profiling
  - mutants-baseline.json:1-8 — mutation baseline present; not a profiler
gaps:
  - Add pprof endpoint or Pyroscope agent for sl-daemon — effort: M
soft_goal_delta: not started

### L46 — Error budgets + SLOs
score: 2  glyph: ~
evidence:
  - docs/ops/observability.md:37-46 — SLO stubs table (availability, ingest, replay, metrics)
  - docs/ops/observability.md:48-57 — error-budget stub policy (budget/burn/exhaustion/reset)
  - docs/ops/observability.md:65-79 — RED mapping tied to SLO signals
  - docs/ops/alerts.md:40-52 — SL-INGEST-ERROR-BUDGET alert stub references SLO
  - MISSING: enforced production SLAs / automated budget burn alerts
gaps:
  - Promote stubs to measured SLIs with burn alerts once exporters land — effort: M
soft_goal_delta: +50% (RED+SLO+budget stubs live; not enforced)

### L47 — Health endpoints
score: 3  glyph: ✓
evidence:
  - crates/sl-daemon/src/http.rs:58-59,89-108 — GET /healthz (liveness) + GET /readyz (out_dir dependency check → 200/503)
  - process-compose.yaml:8-11 — readiness_probe http_get path=/readyz port=8080
  - docs/ops/runbook.md:41-65 — healthz vs readyz operator checks + failure triage
  - docs/ops/observability.md:19-35 — probe semantics table + rules of thumb
  - docs/functional_requirements.md:27 — FR-014 maps /healthz + /readyz + process-compose
gaps:
  - Extend /readyz with future dep checks (DB/object store) when adapters land — effort: S
soft_goal_delta: +15% (both probes + out_dir check complete; multi-dep future)

### L48 — Alerting routing
score: 1  glyph: △
evidence:
  - docs/ops/alerts.md:1-38 — alert stub sketches (HEALTHZ/READYZ) with severity labels + runbook links
  - docs/ops/observability.md:84-97 — severity/routing stub table (Slack/PagerDuty TBD)
  - docs/ops/runbook.md:83-86 — common-failure triage table
  - MISSING: live PagerDuty / OpsGenie / Slack webhook wiring
gaps:
  - Wire Slack/PagerDuty routes and load alert stubs into a real system — effort: L
soft_goal_delta: +35% (severity+runbook stubs; no paging)

### L49 — Dashboard coverage
score: 0  glyph: ✗
evidence:
  - MISSING: grafana / datadog / dashboard JSON — repo search (excl. audit/) returned 0 matches
  - crates/sl-daemon/src/metrics.rs:6-13 — MetricsSummary is API JSON only; no dashboard export
  - docs/ops/observability.md:61 — explicitly no Prometheus scrape endpoint yet
gaps:
  - Ship Grafana dashboard for daemon RED metrics — effort: M
soft_goal_delta: not started

### L50 — Chaos/load testing
score: 0  glyph: ✗
evidence:
  - MISSING: chaos / load-test / steady-state hypothesis harness — repo search (excl. audit/) returned 0 matches
  - crates/sl-daemon/tests/pipeline.rs:1-6 — deterministic pipeline test; not load/chaos
  - .github/workflows/ci.yml:28-29 — cargo test only; no load job
gaps:
  - Add load script + monthly chaos/game-day cadence — effort: L
soft_goal_delta: not started

CLUSTER_TOTAL score=11/30 pct=37% grade=F
CLUSTER_DONE cluster=C05 repo=SessionLedger
