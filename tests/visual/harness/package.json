{
  "name": "sessionledger-visual-harness",
  "private": true,
  "type": "module",
  "scripts": {
    "check:contract": "pwsh -NoProfile -File ./validate.ps1",
    "test:visual": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
