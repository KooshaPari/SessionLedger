[package]
name = "sl-schema"
version = "0.1.0"
edition = "2021"
description = "Typed row structs for SessionLedger's SQLite schema"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
