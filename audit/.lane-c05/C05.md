CLUSTER_START cluster=C05 repo=SessionLedger pillars=L41-L50 date=2026-07-08

### L41 — Structured logs
score: 1  glyph: △
evidence:
  - src/ports/mod.rs:68-71 — TraceSink trait is a stub (`fn span(&self, name: &str)`) with no JSON/logfmt impl
  - docs/DESIGN.md:154 — observability mapped to pheno-tracing / structured logs as future composition
  - MISSING: tracing / slog / tracing-subscriber crates in Cargo.toml:22-34
  - crates/sl-daemon/src/http.rs:84-87 — healthz returns plain "ok"; no structured request logging
gaps:
  - Wire tracing + JSON subscriber in sl-daemon (logfmt-pretty for dev) — effort: M
soft_goal_delta: +10% toward soft goal (port stub only)

### L42 — OpenTelemetry instrumentation
score: 1  glyph: △
evidence:
  - src/ports/mod.rs:7-8 — TraceSink documented as pheno-tracing TracePort boundary
  - docs/DESIGN.md:154 — Observability row cites pheno-tracing / PhenoObservability; no OTLP exporter
  - docs/SESSION_ARTIFACT_STANDARD.md:45-77 — discusses OTel GenAI semconv and explicitly defers to TraceSink port
  - MISSING: opentelemetry / otlp dependency or SDK wiring in Cargo.toml / crates/*/Cargo.toml
gaps:
  - Add OTel SDK + OTLP exporter behind TraceSink adapter — effort: L
soft_goal_delta: +15% toward soft goal (port designed; SDK absent)

### L43 — Metrics emission
score: 1  glyph: △
evidence:
  - crates/sl-daemon/src/metrics.rs:1-29 — GET /api/metrics computes bundle/token/model histograms (app stats, not RED/USE)
  - crates/sl-daemon/src/http.rs:62 — routes /api/metrics handler
  - README.md:36 — documents GET /api/metrics session statistics
  - MISSING: prometheus / opentelemetry-metrics / statsd exporters in crate manifests
gaps:
  - Emit Prometheus/OTLP counters+gauges (RED/USE) alongside app metrics — effort: M
soft_goal_delta: +20% toward soft goal (domain metrics endpoint only)

### L44 — Distributed trace propagation
score: 0  glyph: ✗
evidence:
  - MISSING: traceparent / tracestate — rg across repo (excl. audit/) returned 0 matches
  - src/ports/mod.rs:68-71 — TraceSink has no context-propagation API
  - crates/sl-daemon/src/http.rs:52-65 — axum router has no W3C trace middleware
gaps:
  - Propagate W3C traceparent across HTTP and pipeline stages — effort: M
soft_goal_delta: not started

### L45 — Continuous profiling
score: 0  glyph: ✗
evidence:
  - MISSING: pyroscope / pprof / continuous profiling — rg across repo (excl. audit/) returned 0 matches
  - .github/workflows/ci.yml:12-41 — no profiling or flamegraph job
  - .github/workflows/nightly-mutants.yml:1-28 — mutation testing only; not profiling
gaps:
  - Add pprof endpoint or Pyroscope agent for sl-daemon — effort: M
soft_goal_delta: not started

### L46 — Error budgets + SLOs
score: 0  glyph: ✗
evidence:
  - MISSING: SLO / error budget / SLI policy docs — rg across repo (excl. audit/) returned 0 matches
  - docs/DESIGN.md:170-182 — phased roadmap has no SLO/error-budget phase
  - .qgate.toml:7-19 — coverage threshold only; not service SLOs
gaps:
  - Define per-service SLOs + error-budget policy for sl-daemon — effort: M
soft_goal_delta: not started

### L47 — Health endpoints
score: 2  glyph: ~
evidence:
  - crates/sl-daemon/src/http.rs:56 — GET /healthz registered
  - crates/sl-daemon/src/http.rs:84-87 — liveness returns 200 "ok"
  - process-compose.yaml:8-11 — readiness_probe hits /healthz (reused as readiness)
  - README.md:33 — documents GET /healthz
  - MISSING: /readyz — rg across repo (excl. audit/) returned 0 matches; no per-dep readiness checks
gaps:
  - Add /readyz with dependency checks distinct from /healthz — effort: S
soft_goal_delta: +50% toward soft goal (liveness present; readyz missing)

### L48 — Alerting routing
score: 0  glyph: ✗
evidence:
  - MISSING: PagerDuty / OpsGenie / Slack alert routing — rg across repo (excl. audit/) returned 0 matches
  - .github/workflows/ — no alert-on-failure notification steps beyond default GH status
  - MISSING: runbooks/ directory or alert severity policy
gaps:
  - Add Slack/PagerDuty routing + severity policy + runbooks — effort: L
soft_goal_delta: not started

### L49 — Dashboard coverage
score: 0  glyph: ✗
evidence:
  - MISSING: grafana / datadog / dashboard JSON — rg across repo (excl. audit/) returned 0 matches
  - crates/sl-daemon/src/metrics.rs:6-13 — MetricsSummary is API JSON only; no dashboard export
  - docs/ — no observability dashboard docs
gaps:
  - Ship Grafana dashboard for daemon RED metrics — effort: M
soft_goal_delta: not started

### L50 — Chaos/load testing
score: 0  glyph: ✗
evidence:
  - MISSING: chaos / load-test / steady-state hypothesis harness — rg across repo (excl. audit/) returned 0 matches
  - crates/sl-daemon/tests/pipeline.rs:1-6 — deterministic pipeline test; not load/chaos
  - .github/workflows/ci.yml:28-29 — cargo test only; no load job
gaps:
  - Add load script + monthly chaos/game-day cadence — effort: L
soft_goal_delta: not started

CLUSTER_TOTAL score=5/30 pct=17% grade=F
CLUSTER_DONE cluster=C05 repo=SessionLedger
