CLUSTER_START cluster=C04 repo=SessionLedger pillars=L31-L40 date=2026-07-05

### L31 — Secrets in repo
score: 1  glyph: △
evidence:
  - .github/workflows/qgate.yml:9 references `sbom` generation in CI, but no secret-scan job is present
  - No matches for `gitleaks` / `trufflehog` / `pre-commit` in repo root search across README.md, docs, .github, Cargo.toml, src, tests, packaging, .qgate.toml
  - .gitignore:1 excludes `.env`, `.claude`, and `target/`, but that is not secret scanning enforcement
gaps:
  - Add `gitleaks` and `trufflehog` to CI and pre-commit hooks — effort: M
soft_goal_delta: secret hygiene is implicit; automated scanning is missing

### L32 — SBOM emitted
score: 1  glyph: △
evidence:
  - .github/workflows/qgate.yml:9 notes `cyclonedx-rust-cargo generates target/sbom.cdx.json`
  - .github/workflows/qgate.yml:66 wires the qgate workflow, but the repo search found no SPDX output path or in-tarball SBOM artifact
  - No `sbom`/`spdx`/`cyclonedx` files exist under the repo root search
  - packaging/README.md:35 mentions release packaging, but not SBOM inclusion
gaps:
  - Emit per-release SPDX + CycloneDX and bundle it with artifacts — effort: M
soft_goal_delta: CI hints at SBOM work, but there is no committed SBOM artifact path

### L33 — Vuln scanning
score: 1  glyph: △
evidence:
  - .github/workflows/qgate.yml:9 references the quality gate but no `cargo audit`, `pip-audit`, or `govulncheck` step appears in the repo search
  - Cargo.toml and Cargo.lock exist, so Rust dependency scanning is applicable
  - No repo matches for `cargo audit` / `pip-audit` / `govulncheck` were found in the source search
  - .github/workflows/ci.yml runs build/test/lint only; no vuln scan job is visible in the workflow file search
gaps:
  - Add per-PR and scheduled vuln scans with SARIF upload — effort: M
soft_goal_delta: dependency graph exists, but vulnerability scanning is not wired in

### L34 — Signed commits
score: 0  glyph: ✗
evidence:
  - No matches for `DCO`, `GPG`, `SSH`, or branch-protection markers in the repo search
  - .github/workflows/ci.yml and .github/workflows/release.yml show standard GitHub Actions usage only
  - No repo-local policy file or template enforces commit signoff
gaps:
  - Add DCO signoff policy and branch protection requirements for signed commits — effort: M
soft_goal_delta: commit authenticity is not enforced in-repo

### L35 — Signed releases
score: 0  glyph: ✗
evidence:
  - .github/workflows/release.yml:1-100 creates release assets, but there is no cosign/minisign/Sigstore step
  - packaging/README.md:35 explicitly says there is no codesigning in this scaffold
  - Repo search found no `cosign`, `sigstore`, `minisign`, or provenance references in workflow or docs
gaps:
  - Add keyless Sigstore signing and provenance attestations for release artifacts — effort: L
soft_goal_delta: release pipeline exists, but artifacts are unsigned

### L36 — 2FA on maintainer accounts
score: 0  glyph: ✗
evidence:
  - Repo search cannot verify maintainer account 2FA status from local files
  - No org policy or maintainer checklist in repo search mentions 2FA enforcement
  - .github/workflows/* contain no account-policy assertions
gaps:
  - Document maintainer 2FA requirement and enforce via org policy outside the repo — effort: S
soft_goal_delta: account security is external to this repo and currently unverified

### L37 — Dependabot/Renovate configured
score: 0  glyph: ✗
evidence:
  - find .github -maxdepth 3 -type f returned no `dependabot.yml` or Renovate config
  - Repo search found no `dependabot` or `renovate` references in workflows or docs
  - .github/workflows/ci.yml and .github/workflows/release.yml do not include update-bot automation
gaps:
  - Add `.github/dependabot.yml` or Renovate config for weekly dependency PRs — effort: S
soft_goal_delta: no automated dependency update bot is configured

### L38 — CVE feed subscribed
score: 0  glyph: ✗
evidence:
  - Repo search found no `GHSA`, `OSV`, `NVD`, or advisory feed references
  - No security-policy or subscription file exists in `.github/` from the repo file scan
  - Workflow files do not mention advisory watches or feed ingestion
gaps:
  - Subscribe the repo/org to GHSA + OSV + NVD feeds and record the policy — effort: S
soft_goal_delta: advisory intake is not documented or automated

### L39 — Threat model documented
score: 0  glyph: ✗
evidence:
  - find . -maxdepth 3 for `*THREAT*` / `*threat*` / `*security*` returned no `THREAT_MODEL.md`
  - docs/DESIGN.md:52 discusses provenance/auditability, but it is not a threat model artifact
  - Repo search found no STRIDE/attack-tree style security doc
gaps:
  - Add `THREAT_MODEL.md` with STRIDE table and attack tree — effort: M
soft_goal_delta: security design notes exist, but no formal threat model is committed

### L40 — Sandbox boundary
score: 1  glyph: △
evidence:
  - .github/workflows/ci.yml:7-17 runs on `ubuntu-latest`, indicating a containerized CI boundary, but no explicit sandbox config is shown
  - .github/workflows/release.yml uses hosted runners and standard shell packaging steps only
  - Repo search found no `seccomp`, `rootless`, `privileged: true`, or Docker isolation policy files
  - process-compose.yaml exists, but the repo search did not surface sandbox directives in the file search output
gaps:
  - Add documented Docker/seccomp/rootless sandbox boundaries for build and release jobs — effort: M
soft_goal_delta: hosted runners provide some isolation, but the sandbox contract is not explicit

CLUSTER_TOTAL score=3/30 pct=10% grade=F
CLUSTER_DONE cluster=C04 repo=SessionLedger
