CLUSTER_START cluster=C06 repo=SessionLedger pillars=L51-L60 date=2026-07-08

### L51 — Pinned dependencies
score: 0  glyph: ✗
evidence:
  - MISSING: Cargo.lock — not present in working tree; `git show HEAD:Cargo.lock` fails (path does not exist)
  - .github/workflows/ci.yml:27-29 — `cargo build/test --locked` requires a committed lockfile that is absent on main
  - Containerfile:4 — `cargo build --release --locked` likewise assumes Cargo.lock
  - AGENTS.md:49 — states "CI uses --locked — keep Cargo.lock committed and current" (policy without artifact)
gaps:
  - Commit Cargo.lock and verify --locked in CI — effort: S
soft_goal_delta: not started (intent documented; lockfile missing)

### L52 — Reproducible builds
score: 0  glyph: ✗
evidence:
  - MISSING: SOURCE_DATE_EPOCH — rg across repo (excl. audit/) returned 0 matches
  - .github/workflows/release.yml:61-85 — release builds binaries without bit-identical/repro-check step
  - CHANGELOG.md:10 — "reproducible toolchain" refers to rust-toolchain.toml, not bit-identical artifacts
  - docs/OKF-ROUNDTRIP.md:8 — "reproducible" describes OKF smoke test, not build hermeticity
gaps:
  - Document SOURCE_DATE_EPOCH + add repro-check CI for release binaries — effort: L
soft_goal_delta: not started

### L53 — Build provenance
score: 0  glyph: ✗
evidence:
  - .github/workflows/release.yml:96-145 — create Release attaches assets; no SLSA/attest/OIDC provenance step
  - MISSING: slsa / attest / provenance generator — rg across workflows returned 0 matches
  - packaging/README.md:33-35 — packaging scaffold without signing/attestation
gaps:
  - Wire SLSA provenance (GitHub OIDC) on release workflow — effort: L
soft_goal_delta: not started

### L54 — Hermetic builds
score: 0  glyph: ✗
evidence:
  - .github/workflows/ci.yml:17-29 — standard checkout + crates.io network build; no offline/hermetic enforcement
  - Containerfile:1-4 — builder stage pulls rust base and resolves deps over network
  - MISSING: hermetic / --offline build policy in CI or docs
gaps:
  - Enforce network-less builds with vendored/cached deps in CI — effort: L
soft_goal_delta: not started

### L55 — Dependency confusion guard
score: 0  glyph: ✗
evidence:
  - Cargo.toml:22-34 — public crates.io deps only; no scoped/private registry config
  - MISSING: .cargo/config.toml registry replace / cargo-deny bans for typosquat
  - MISSING: dependency confusion / private mirror policy — rg across repo (excl. audit/) returned 0 matches
gaps:
  - Add scoped registry policy + typosquat checks (cargo-deny) — effort: M
soft_goal_delta: not started

### L56 — Container provenance
score: 1  glyph: △
evidence:
  - Containerfile:1-11 — image build definition for sl-daemon exists
  - README.md:40-44 — podman build/run documented
  - MISSING: cosign sign / attest / verify-on-deploy in .github/workflows/
  - .github/workflows/release.yml:1-145 — releases desktop binaries, not signed container images
gaps:
  - Sign+attest container images in CI and verify on deploy — effort: L
soft_goal_delta: +10% toward soft goal (image exists; unsigned)

### L57 — MCP server provenance
score: 0  glyph: ✗
evidence:
  - NOT_APPLICABLE_SOFT: SessionLedger is a session compiler/daemon, not an MCP host
  - MISSING: mcp / phenoMCP pin files — rg across repo (excl. audit/) returned 0 matches
  - AGENTS.md:1-50 — agent entrypoint has no MCP server pin list
gaps:
  - none — not applicable unless MCP servers are introduced — effort: S
soft_goal_delta: n/a

### L58 — License scan
score: 0  glyph: ✗
evidence:
  - MISSING: deny.toml
  - MISSING: cargo-deny / license allow-list CI step — rg in .github returned 0 matches
  - LICENSE-MIT:1 / LICENSE-APACHE:1 — dual license present, but no automated copyleft scan
  - .github/workflows/ci.yml:12-41 — no license-policy job
gaps:
  - Add deny.toml + every-PR cargo-deny with deny-on-violation — effort: M
soft_goal_delta: not started

### L59 — Source code provenance
score: 2  glyph: ~
evidence:
  - git cat-file -p HEAD — commits carry gpgsig PGP signatures (GitHub merge signing)
  - git log -10 --format='%G?' — recent history shows signed commits (E = key not in local trust)
  - CODEOWNERS:1-3 — @KooshaPari owns tree; no branch-protection-requires-signed-commits file in-repo
  - CONTRIBUTING.md:18-21 — branch discipline documented; no per-author signing mandate
gaps:
  - Require signed commits for all authors via branch protection — effort: M
soft_goal_delta: +40% toward soft goal (signing present; policy incomplete)

### L60 — Toolchain pinning
score: 2  glyph: ~
evidence:
  - rust-toolchain.toml:1-5 — channel=stable with rustfmt/clippy components
  - Cargo.toml:12 — rust-version = "1.85" MSRV pin
  - AGENTS.md:46 — documents MSRV via rust-toolchain.toml
  - .github/workflows/qgate.yml:31 — dtolnay/rust-toolchain pinned by commit SHA
  - .github/workflows/ci.yml:18 — uses dtolnay/rust-toolchain@stable (not SHA-pinned action; channel not file-verified)
gaps:
  - Pin toolchain channel to exact version + verify SHA256 in CI — effort: S
soft_goal_delta: +60% toward soft goal (file+CI present; not full SHA verify)

CLUSTER_TOTAL score=5/30 pct=17% grade=F
CLUSTER_DONE cluster=C06 repo=SessionLedger
