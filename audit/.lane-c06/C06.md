CLUSTER_START cluster=C06 repo=SessionLedger pillars=L51-L60 date=2026-07-10

### L51 — Pinned dependencies
score: 2  glyph: ~
evidence:
  - Cargo.lock:1-5 — workspace lockfile committed (version=4)
  - crates/sl-daemon/Cargo.lock — daemon lockfile present (excluded workspace member)
  - .github/workflows/ci.yml:27-29 — cargo build/test --locked verifies lockfile on PR
  - AGENTS.md:53 — documents CI --locked discipline
  - MISSING: cargo update --locked gate / lockfile drift bot beyond Dependabot
gaps:
  - Add lockfile drift check (cargo update --locked dry-run) in CI — effort: S
soft_goal_delta: +60% (lockfile+--locked verify live; update-locked gate absent)

### L52 — Reproducible builds
score: 0  glyph: ✗
evidence:
  - MISSING: SOURCE_DATE_EPOCH — repo search (excl. audit/) returned 0 matches
  - .github/workflows/release.yml:61-85 — release builds binaries without bit-identical/repro-check step
  - CHANGELOG.md — toolchain notes do not establish bit-identical artifacts
gaps:
  - Document SOURCE_DATE_EPOCH + add repro-check CI for release binaries — effort: L
soft_goal_delta: not started

### L53 — Build provenance
score: 0  glyph: ✗
evidence:
  - .github/workflows/release.yml:16-18 — signing/attestation deferred to issue #66
  - MISSING: slsa / attest / provenance generator — no matches in .github/workflows/
  - packaging/README.md — packaging scaffold without signing/attestation
gaps:
  - Wire SLSA provenance (GitHub OIDC) on release workflow — effort: L
soft_goal_delta: not started

### L54 — Hermetic builds
score: 0  glyph: ✗
evidence:
  - .github/workflows/ci.yml:17-29 — standard checkout + crates.io network build; no offline/hermetic enforcement
  - Containerfile:1-4 — builder stage pulls rust base and resolves deps over network
  - vendor/ — only webkit2gtk stubs; not a full cargo vendor tree for --offline
  - MISSING: hermetic / --offline build policy in CI or docs
gaps:
  - Enforce network-less builds with vendored/cached deps in CI — effort: L
soft_goal_delta: not started

### L55 — Dependency confusion guard
score: 2  glyph: ~
evidence:
  - deny.toml:31-35 — unknown-registry=deny; allow-registry crates.io only; allow-git KooshaPari
  - .github/workflows/security.yml:15-25 — cargo deny check enforces sources policy on PR
  - Cargo.toml:1-5 — workspace members crates/sl-viewer; sl-daemon excluded as own root
  - MISSING: typosquat allow/deny name lists beyond registry allow-list
gaps:
  - Add typosquat deny list / private mirror policy if private crates appear — effort: M
soft_goal_delta: +50% (sources policy enforced; typosquat list thin)

### L56 — Container provenance
score: 1  glyph: △
evidence:
  - Containerfile:1-11 — image build definition for sl-daemon exists
  - crates/sl-daemon/Containerfile:41 — non-root USER sl
  - README.md:40-44 — podman build/run documented
  - MISSING: cosign sign / attest / verify-on-deploy in .github/workflows/
gaps:
  - Sign+attest container images in CI and verify on deploy — effort: L
soft_goal_delta: +15% toward soft goal (image exists; unsigned)

### L57 — MCP server provenance
score: 0  glyph: ✗
evidence:
  - NOT_APPLICABLE: SessionLedger is a session compiler/daemon, not an MCP host
  - MISSING: mcp / phenoMCP pin files — repo search (excl. audit/) returned 0 matches
  - AGENTS.md:1-53 — agent entrypoint has no MCP server pin list
gaps:
  - none — not applicable unless MCP servers are introduced — effort: S
soft_goal_delta: n/a

### L58 — License scan
score: 3  glyph: ✓
evidence:
  - deny.toml:15-29 — [licenses] allow-list (MIT/Apache/BSD/ISC/Unicode/Zlib/CC0) confidence-threshold 0.9
  - .github/workflows/security.yml:15-25 — every-PR cargo deny check --config deny.toml
  - LICENSE-MIT:1 / LICENSE-APACHE:1 — dual license present
  - SECURITY.md:43 — documents deny.toml enforcement via security.yml
gaps:
  - none identified — effort: S
soft_goal_delta: +5% toward copyleft exception process docs

### L59 — Source code provenance
score: 2  glyph: ~
evidence:
  - CODEOWNERS:1-3 — @KooshaPari owns tree; no branch-protection-requires-signed-commits file in-repo
  - CONTRIBUTING.md:18-21 — branch discipline documented; no per-author signing mandate
  - CODE_OF_CONDUCT.md:1 — Contributor Covenant present (community provenance baseline)
  - MISSING: in-repo policy requiring signed commits for all authors
gaps:
  - Require signed commits for all authors via branch protection — effort: M
soft_goal_delta: +40% toward soft goal (ownership present; policy incomplete)

### L60 — Toolchain pinning
score: 2  glyph: ~
evidence:
  - rust-toolchain.toml:1-5 — channel=stable with rustfmt/clippy components
  - Cargo.toml:12 — rust-version = "1.85" MSRV pin
  - AGENTS.md:50 — documents MSRV via rust-toolchain.toml
  - .github/workflows/ci.yml:18-20 — dtolnay/rust-toolchain@stable with clippy/rustfmt (honors toolchain file)
  - .github/workflows/qgate.yml:31 — dtolnay/rust-toolchain pinned by commit SHA
  - MISSING: exact channel version pin + SHA256 verify of toolchain bits
gaps:
  - Pin toolchain channel to exact version + verify SHA256 in CI — effort: S
soft_goal_delta: +65% toward soft goal (file+CI present; not full SHA verify)

CLUSTER_TOTAL score=12/30 pct=40% grade=D
CLUSTER_DONE cluster=C06 repo=SessionLedger
