# Clippy configuration values belong in this file; lint levels and per-lint
# exceptions are declared in Cargo.toml's `[lints.clippy]` table.  Older
# Clippy versions accepted `warn`/`allow` here as lint lists, but current
# Clippy parses those keys as configuration options and rejects them.
